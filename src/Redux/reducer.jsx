
const initState = {
    search: {
        loading: false,
        error: false,
        data: []
    }
}

export const reducer = (state = initState, {type, payload}) => {
    switch (type) {
        case SEARCH:
            
    }

}